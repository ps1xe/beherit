syntax = "proto3";

package sounds;

service SoundsService{
    rpc UploadSound (UploadSoundRequest) returns (Void) {}
}

//UploadSound

message UploadSoundRequest{
    bytes buffer = 1;
    string userId = 2;
}

//Common massages

message Void{}